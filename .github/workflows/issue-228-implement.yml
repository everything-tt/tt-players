name: Issue 228 implementation helper

on:
  push:
    branches:
      - agent/issue-228-england-territory

permissions:
  contents: write

jobs:
  implement:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/issue-228-england-territory
          fetch-depth: 0

      - name: Apply England territory migration
        shell: bash
        run: |
          set -euo pipefail
          python3 <<'PY'
          from pathlib import Path
          import json

          root = Path('.')
          bootstrap_path = root / 'apps/worker/src/bootstrap.ts'
          bootstrap = bootstrap_path.read_text()

          replacements = {
              'interface TT365Division {': 'export interface TT365Division {',
              'interface TTLeaguesDivision {': 'export interface TTLeaguesDivision {',
              'interface HistoryConfig {': 'export interface HistoryConfig {',
              'interface LeagueConfig {': 'export interface LeagueConfig {',
              'function readLeagueConfigs(): LeagueConfig[] {': 'export function readLegacyLeagueConfigs(): LeagueConfig[] {',
          }
          for old, new in replacements.items():
              if old not in bootstrap:
                  raise SystemExit(f'missing bootstrap marker: {old}')
              bootstrap = bootstrap.replace(old, new, 1)

          old_bootstrap_start = '''export async function bootstrap(
              db: Kysely<Database>,
              options: BootstrapOptions = {},
          ): Promise<ScrapeTarget[]> {
              const { includeHistory = false, leagueNames } = options;
              const leagueNameFilter = leagueNames && leagueNames.length > 0
                  ? new Set(leagueNames)
                  : null;
              const leagues = readLeagueConfigs().filter((league) =>
                  leagueNameFilter ? leagueNameFilter.has(league.leagueName) : true,
              );

              const targets: ScrapeTarget[] = [];
          '''
          new_bootstrap_start = '''export async function bootstrapLeagueConfigs(
              db: Kysely<Database>,
              leagueConfigs: LeagueConfig[],
              options: BootstrapOptions = {},
          ): Promise<ScrapeTarget[]> {
              const { includeHistory = false, leagueNames } = options;
              const leagueNameFilter = leagueNames && leagueNames.length > 0
                  ? new Set(leagueNames)
                  : null;
              const leagues = leagueConfigs.filter((league) =>
                  leagueNameFilter ? leagueNameFilter.has(league.leagueName) : true,
              );

              const targets: ScrapeTarget[] = [];
          '''
          if old_bootstrap_start not in bootstrap:
              raise SystemExit('missing bootstrap function start')
          bootstrap = bootstrap.replace(old_bootstrap_start, new_bootstrap_start, 1)

          marker = '''    return targets;
          }

          // ─── Upsert helpers ───────────────────────────────────────────────────────────
          '''
          replacement = '''    return targets;
          }

          /**
           * Backwards-compatible legacy bootstrap. Production target resolution can pass
           * territory-owned configs through bootstrapLeagueConfigs while older scripts and
           * parity tests can still evaluate the legacy files directly.
           */
          export async function bootstrap(
              db: Kysely<Database>,
              options: BootstrapOptions = {},
          ): Promise<ScrapeTarget[]> {
              return bootstrapLeagueConfigs(db, readLegacyLeagueConfigs(), options);
          }

          // ─── Upsert helpers ───────────────────────────────────────────────────────────
          '''
          if marker not in bootstrap:
              raise SystemExit('missing bootstrap wrapper insertion marker')
          bootstrap = bootstrap.replace(marker, replacement, 1)
          bootstrap_path.write_text(bootstrap)

          territory_path = root / 'apps/worker/src/territory-source-catalog.ts'
          territory_path.write_text(r'''import { readdirSync, readFileSync } from 'node:fs';
          import { dirname, resolve } from 'node:path';
          import { fileURLToPath } from 'node:url';
          import type { Kysely } from 'kysely';
          import type { Database } from '@tt-players/db';
          import type {
              HistoryConfig,
              LeagueConfig,
              TT365Division,
              TTLeaguesDivision,
          } from './bootstrap.js';
          import { SOURCE_RESOURCE_TYPES, type SourceResourceType } from './sources/adapter.js';
          import { upsertSourceInstance, upsertSourceResource } from './sources/registry.js';

          const __dirname = dirname(fileURLToPath(import.meta.url));
          const TERRITORY_CONFIG_DIR = resolve(__dirname, '../config/territories');
          const RESOURCE_TYPES = new Set<string>(SOURCE_RESOURCE_TYPES);
          const SOURCE_STATUSES = new Set<string>(['active', 'discovery', 'blocked', 'external']);
          const INGESTION_MODES = new Set<string>(['catalog-only', 'legacy-config', 'league-config']);
          const TT365_PLATFORM_NAME = 'TableTennis365';
          const TT365_PLATFORM_BASE_URL = 'https://www.tabletennis365.com';
          const TTLEAGUES_PLATFORM_NAME = 'TT Leagues';
          const TTLEAGUES_PLATFORM_BASE_URL = 'https://ttleagues-api.azurewebsites.net/api';

          export type TerritorySourceStatus = 'active' | 'discovery' | 'blocked' | 'external';
          export type TerritoryIngestionMode = 'catalog-only' | 'legacy-config' | 'league-config';

          export interface TerritoryCatalogOnlyIngestionConfig {
              mode: 'catalog-only';
          }

          export interface TerritoryLegacyIngestionConfig {
              mode: 'legacy-config';
              legacyLeagueName: string;
          }

          export interface TerritoryLeagueIngestionConfig {
              mode: 'league-config';
              externalId: string;
              seasonName: string;
              seasonExtId: string;
              history?: HistoryConfig;
              regions?: string[];
              divisions: Array<TT365Division | TTLeaguesDivision>;
          }

          export type TerritoryIngestionConfig =
              | TerritoryCatalogOnlyIngestionConfig
              | TerritoryLegacyIngestionConfig
              | TerritoryLeagueIngestionConfig;

          export interface TerritoryResourceConfig {
              resourceType: SourceResourceType;
              externalId: string;
              name?: string | null;
              publicUrl?: string | null;
              enabled?: boolean;
              refreshPolicy?: unknown;
          }

          export interface TerritorySourceConfig {
              key: string;
              name: string;
              platformName: string;
              platformBaseUrl: string;
              baseUrl: string;
              adapterKey: string;
              status: TerritorySourceStatus;
              enabled?: boolean;
              ingestion?: TerritoryIngestionConfig;
              notes?: string;
              config?: Record<string, unknown>;
              resources?: TerritoryResourceConfig[];
          }

          export interface TerritoryManifest {
              territory: string;
              sources: TerritorySourceConfig[];
          }

          export interface TerritoryCatalogBootstrapResult {
              sourceCount: number;
              enabledLegacyLeagueNames: string[];
          }

          interface TerritoryLogger {
              info?: (message: string) => void;
          }

          function assertNonEmpty(value: unknown, field: string): asserts value is string {
              if (typeof value !== 'string' || value.trim().length === 0) {
                  throw new Error(`Territory source catalog ${field} must be a non-empty string`);
              }
          }

          function assertOptionalBoolean(value: unknown, field: string): void {
              if (value !== undefined && typeof value !== 'boolean') {
                  throw new Error(`Territory source catalog ${field} must be a boolean`);
              }
          }

          function assertNonNegativeInteger(value: unknown, field: string): void {
              if (!Number.isInteger(value) || Number(value) < 0) {
                  throw new Error(`Territory source catalog ${field} must be a non-negative integer`);
              }
          }

          function assertPositiveInteger(value: unknown, field: string): void {
              if (!Number.isInteger(value) || Number(value) <= 0) {
                  throw new Error(`Territory source catalog ${field} must be a positive integer`);
              }
          }

          function assertHttpUrl(value: unknown, field: string): asserts value is string {
              assertNonEmpty(value, field);
              let parsed: URL;
              try {
                  parsed = new URL(value);
              } catch {
                  throw new Error(`Territory source catalog ${field} must be an absolute URL`);
              }
              if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') {
                  throw new Error(`Territory source catalog ${field} must use http or https`);
              }
          }

          function validateHistory(history: unknown, prefix: string): void {
              if (history === undefined) return;
              if (!history || typeof history !== 'object' || Array.isArray(history)) {
                  throw new Error(`Territory source catalog ${prefix} must be an object`);
              }
              const typed = history as Partial<HistoryConfig>;
              assertOptionalBoolean(typed.enabled, `${prefix}.enabled`);
              if (typed.maxSeasons !== undefined) {
                  assertNonNegativeInteger(typed.maxSeasons, `${prefix}.maxSeasons`);
              }
              assertOptionalBoolean(typed.includeCups, `${prefix}.includeCups`);
          }

          function validateLeagueIngestion(
              source: TerritorySourceConfig,
              ingestion: TerritoryLeagueIngestionConfig,
              prefix: string,
          ): void {
              assertNonEmpty(ingestion.externalId, `${prefix}.externalId`);
              assertNonEmpty(ingestion.seasonName, `${prefix}.seasonName`);
              assertNonEmpty(ingestion.seasonExtId, `${prefix}.seasonExtId`);
              validateHistory(ingestion.history, `${prefix}.history`);

              if (ingestion.regions !== undefined) {
                  if (!Array.isArray(ingestion.regions)) {
                      throw new Error(`Territory source catalog ${prefix}.regions must be an array`);
                  }
                  for (const [index, region] of ingestion.regions.entries()) {
                      assertNonEmpty(region, `${prefix}.regions[${index}]`);
                  }
              }

              if (!Array.isArray(ingestion.divisions)) {
                  throw new Error(`Territory source catalog ${prefix}.divisions must be an array`);
              }

              if (source.adapterKey === 'tt365') {
                  if (
                      source.platformName !== TT365_PLATFORM_NAME
                      || source.platformBaseUrl !== TT365_PLATFORM_BASE_URL
                  ) {
                      throw new Error(
                          `Territory source catalog ${prefix} has inconsistent TableTennis365 platform metadata`,
                      );
                  }
                  const divisionKeys = new Set<string>();
                  for (const [index, division] of ingestion.divisions.entries()) {
                      const divisionPrefix = `${prefix}.divisions[${index}]`;
                      if (!division || typeof division !== 'object' || Array.isArray(division)) {
                          throw new Error(`Territory source catalog ${divisionPrefix} must be an object`);
                      }
                      const typed = division as Partial<TT365Division>;
                      assertNonEmpty(typed.name, `${divisionPrefix}.name`);
                      assertNonEmpty(typed.season, `${divisionPrefix}.season`);
                      assertNonEmpty(typed.slug, `${divisionPrefix}.slug`);
                      const key = typed.slug.toLowerCase();
                      if (divisionKeys.has(key)) {
                          throw new Error(`Territory source catalog ${divisionPrefix}.slug is duplicated`);
                      }
                      divisionKeys.add(key);
                  }
                  return;
              }

              if (source.adapterKey === 'ttleagues') {
                  if (
                      source.platformName !== TTLEAGUES_PLATFORM_NAME
                      || source.platformBaseUrl !== TTLEAGUES_PLATFORM_BASE_URL
                  ) {
                      throw new Error(
                          `Territory source catalog ${prefix} has inconsistent TT Leagues platform metadata`,
                      );
                  }
                  const divisionKeys = new Set<number>();
                  for (const [index, division] of ingestion.divisions.entries()) {
                      const divisionPrefix = `${prefix}.divisions[${index}]`;
                      if (!division || typeof division !== 'object' || Array.isArray(division)) {
                          throw new Error(`Territory source catalog ${divisionPrefix} must be an object`);
                      }
                      const typed = division as Partial<TTLeaguesDivision>;
                      assertNonEmpty(typed.name, `${divisionPrefix}.name`);
                      assertPositiveInteger(typed.divisionId, `${divisionPrefix}.divisionId`);
                      const key = typed.divisionId as number;
                      if (divisionKeys.has(key)) {
                          throw new Error(`Territory source catalog ${divisionPrefix}.divisionId is duplicated`);
                      }
                      divisionKeys.add(key);
                  }
                  return;
              }

              throw new Error(
                  `Territory source catalog ${prefix} uses league-config with unsupported adapter ${source.adapterKey}`,
              );
          }

          export function parseTerritoryManifest(raw: string, filename = '<memory>'): TerritoryManifest {
              const parsed = JSON.parse(raw) as Partial<TerritoryManifest> | null;
              if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) {
                  throw new Error(`Territory source catalog ${filename} must contain an object`);
              }

              assertNonEmpty(parsed.territory, `${filename}.territory`);
              if (!Array.isArray(parsed.sources)) {
                  throw new Error(`Territory source catalog ${filename}.sources must be an array`);
              }

              const sourceKeys = new Set<string>();
              const concreteLeagueIds = new Set<string>();
              for (const [sourceIndex, source] of parsed.sources.entries()) {
                  const prefix = `${filename}.sources[${sourceIndex}]`;
                  if (!source || typeof source !== 'object' || Array.isArray(source)) {
                      throw new Error(`Territory source catalog ${prefix} must be an object`);
                  }

                  assertNonEmpty(source.key, `${prefix}.key`);
                  if (sourceKeys.has(source.key)) {
                      throw new Error(`Territory source catalog ${prefix}.key is duplicated: ${source.key}`);
                  }
                  sourceKeys.add(source.key);

                  assertNonEmpty(source.name, `${prefix}.name`);
                  assertNonEmpty(source.platformName, `${prefix}.platformName`);
                  assertHttpUrl(source.platformBaseUrl, `${prefix}.platformBaseUrl`);
                  assertHttpUrl(source.baseUrl, `${prefix}.baseUrl`);
                  assertNonEmpty(source.adapterKey, `${prefix}.adapterKey`);
                  if (!SOURCE_STATUSES.has(source.status ?? '')) {
                      throw new Error(`Territory source catalog ${prefix}.status is invalid`);
                  }
                  assertOptionalBoolean(source.enabled, `${prefix}.enabled`);

                  const ingestion = source.ingestion;
                  if (ingestion !== undefined) {
                      if (!ingestion || typeof ingestion !== 'object' || Array.isArray(ingestion)) {
                          throw new Error(`Territory source catalog ${prefix}.ingestion must be an object`);
                      }
                      if (!INGESTION_MODES.has(ingestion.mode ?? '')) {
                          throw new Error(`Territory source catalog ${prefix}.ingestion.mode is invalid`);
                      }
                      if (ingestion.mode === 'legacy-config') {
                          assertNonEmpty(
                              ingestion.legacyLeagueName,
                              `${prefix}.ingestion.legacyLeagueName`,
                          );
                      } else if (ingestion.mode === 'league-config') {
                          validateLeagueIngestion(source, ingestion, `${prefix}.ingestion`);
                          if (concreteLeagueIds.has(ingestion.externalId)) {
                              throw new Error(
                                  `Territory source catalog ${prefix}.ingestion.externalId is duplicated: `
                                  + ingestion.externalId,
                              );
                          }
                          concreteLeagueIds.add(ingestion.externalId);
                      }
                  }

                  if (
                      source.enabled === true
                      && ingestion?.mode !== 'legacy-config'
                      && ingestion?.mode !== 'league-config'
                  ) {
                      throw new Error(
                          `Territory source catalog ${prefix} cannot be enabled without a schedulable ingestion mode`,
                      );
                  }

                  if (source.config !== undefined && (
                      !source.config || typeof source.config !== 'object' || Array.isArray(source.config)
                  )) {
                      throw new Error(`Territory source catalog ${prefix}.config must be an object`);
                  }

                  if (source.resources !== undefined && !Array.isArray(source.resources)) {
                      throw new Error(`Territory source catalog ${prefix}.resources must be an array`);
                  }

                  const resourceKeys = new Set<string>();
                  for (const [resourceIndex, resource] of (source.resources ?? []).entries()) {
                      const resourcePrefix = `${prefix}.resources[${resourceIndex}]`;
                      if (!resource || typeof resource !== 'object' || Array.isArray(resource)) {
                          throw new Error(`Territory source catalog ${resourcePrefix} must be an object`);
                      }
                      if (!RESOURCE_TYPES.has(resource.resourceType)) {
                          throw new Error(
                              `Territory source catalog ${resourcePrefix}.resourceType is invalid: ${resource.resourceType}`,
                          );
                      }
                      assertNonEmpty(resource.externalId, `${resourcePrefix}.externalId`);
                      assertOptionalBoolean(resource.enabled, `${resourcePrefix}.enabled`);
                      if (resource.publicUrl != null) {
                          assertHttpUrl(resource.publicUrl, `${resourcePrefix}.publicUrl`);
                      }

                      const resourceKey = `${resource.resourceType}:${resource.externalId}`;
                      if (resourceKeys.has(resourceKey)) {
                          throw new Error(
                              `Territory source catalog ${resourcePrefix} duplicates resource ${resourceKey}`,
                          );
                      }
                      resourceKeys.add(resourceKey);
                  }

                  if (ingestion?.mode === 'league-config') {
                      const hasLeagueResource = (source.resources ?? []).some(
                          (resource) => resource.resourceType === 'league'
                              && resource.externalId === ingestion.externalId,
                      );
                      if (!hasLeagueResource) {
                          throw new Error(
                              `Territory source catalog ${prefix} league-config requires a matching league resource`,
                          );
                      }
                  }
              }

              return parsed as TerritoryManifest;
          }

          export function readTerritoryManifests(): TerritoryManifest[] {
              let filenames: string[];
              try {
                  filenames = readdirSync(TERRITORY_CONFIG_DIR)
                      .filter((filename) => filename.endsWith('.json'))
                      .sort();
              } catch (error) {
                  const message = error instanceof Error ? error.message : String(error);
                  if (/no such file/i.test(message)) return [];
                  throw error;
              }

              const manifests = filenames.map((filename) => parseTerritoryManifest(
                  readFileSync(resolve(TERRITORY_CONFIG_DIR, filename), 'utf8'),
                  filename,
              ));

              const platforms = new Map<string, string>();
              const sourceIdentities = new Set<string>();
              const leagueExternalIds = new Set<string>();
              for (const manifest of manifests) {
                  for (const source of manifest.sources) {
                      const existingBaseUrl = platforms.get(source.platformName);
                      if (existingBaseUrl && existingBaseUrl !== source.platformBaseUrl) {
                          throw new Error(
                              `Territory source catalog platform ${source.platformName} has conflicting base URLs: `
                              + `${existingBaseUrl} vs ${source.platformBaseUrl}`,
                          );
                      }
                      platforms.set(source.platformName, source.platformBaseUrl);

                      const sourceIdentity = `${source.platformName}:${source.key}`;
                      if (sourceIdentities.has(sourceIdentity)) {
                          throw new Error(
                              `Territory source catalog source identity is duplicated across manifests: ${sourceIdentity}`,
                          );
                      }
                      sourceIdentities.add(sourceIdentity);

                      if (source.ingestion?.mode === 'league-config') {
                          const externalId = source.ingestion.externalId;
                          if (leagueExternalIds.has(externalId)) {
                              throw new Error(
                                  `Territory source catalog league externalId is duplicated across manifests: ${externalId}`,
                              );
                          }
                          leagueExternalIds.add(externalId);
                      }
                  }
              }

              return manifests;
          }

          export function territoryManifestToLeagueConfigs(manifest: TerritoryManifest): LeagueConfig[] {
              const leagueConfigs: LeagueConfig[] = [];
              for (const source of manifest.sources) {
                  if (source.ingestion?.mode !== 'league-config' || source.enabled !== true) continue;
                  const ingestion = source.ingestion;
                  const platform = source.adapterKey === 'tt365' ? 'tt365' : 'ttleagues';
                  const league: LeagueConfig = {
                      platform,
                      leagueName: source.name,
                      externalId: ingestion.externalId,
                      seasonName: ingestion.seasonName,
                      seasonExtId: ingestion.seasonExtId,
                      baseUrl: source.baseUrl,
                      divisions: ingestion.divisions.map((division) => ({ ...division })),
                  };
                  if (ingestion.history !== undefined) league.history = { ...ingestion.history };
                  if (ingestion.regions !== undefined) league.regions = [...ingestion.regions];
                  leagueConfigs.push(league);
              }
              return leagueConfigs;
          }

          export function readTerritoryLeagueConfigs(): LeagueConfig[] {
              return readTerritoryManifests().flatMap(territoryManifestToLeagueConfigs);
          }

          export function readTerritoryOwnedLeagueExternalIds(): string[] {
              const externalIds = new Set<string>();
              for (const manifest of readTerritoryManifests()) {
                  for (const source of manifest.sources) {
                      if (source.ingestion?.mode === 'league-config') {
                          externalIds.add(source.ingestion.externalId);
                      }
                  }
              }
              return Array.from(externalIds).sort();
          }

          async function upsertPlatform(
              db: Kysely<Database>,
              name: string,
              baseUrl: string,
          ): Promise<string> {
              const existing = await db
                  .selectFrom('platforms')
                  .select(['id', 'base_url'])
                  .where('name', '=', name)
                  .executeTakeFirst();

              if (existing) {
                  if (existing.base_url !== baseUrl) {
                      throw new Error(
                          `Territory source catalog platform ${name} has conflicting base URLs: `
                          + `${existing.base_url} vs ${baseUrl}`,
                      );
                  }
                  return existing.id;
              }

              const row = await db
                  .insertInto('platforms')
                  .values({ name, base_url: baseUrl })
                  .returning('id')
                  .executeTakeFirstOrThrow();
              return row.id;
          }

          async function findExistingLeagueId(
              db: Kysely<Database>,
              platformId: string,
              source: TerritorySourceConfig,
          ): Promise<string | null> {
              if (source.ingestion?.mode !== 'league-config') return null;
              const league = await db
                  .selectFrom('leagues')
                  .select('id')
                  .where('platform_id', '=', platformId)
                  .where('external_id', '=', source.ingestion.externalId)
                  .executeTakeFirst();
              return league?.id ?? null;
          }

          /**
           * Registers official territory data sources, including sources that are known but
           * intentionally not schedulable yet. `league-config` sources carry concrete target
           * configuration; `legacy-config` remains a temporary compatibility bridge for
           * territories that have not yet migrated their target definitions.
           */
          export async function bootstrapTerritorySourceCatalog(
              db: Kysely<Database>,
              options: { logger?: TerritoryLogger } = {},
          ): Promise<TerritoryCatalogBootstrapResult> {
              const manifests = readTerritoryManifests();
              let sourceCount = 0;
              const enabledLegacyLeagueNames = new Set<string>();

              for (const manifest of manifests) {
                  for (const source of manifest.sources) {
                      const platformId = await upsertPlatform(db, source.platformName, source.platformBaseUrl);
                      const enabled = source.enabled ?? false;
                      const ingestionMode = source.ingestion?.mode ?? 'catalog-only';

                      if (enabled && source.ingestion?.mode === 'legacy-config') {
                          enabledLegacyLeagueNames.add(source.ingestion.legacyLeagueName);
                      }

                      const sourceInstance = await upsertSourceInstance(db, {
                          platformId,
                          key: source.key,
                          name: source.name,
                          baseUrl: source.baseUrl,
                          adapterKey: source.adapterKey,
                          enabled,
                          config: {
                              territory: manifest.territory,
                              status: source.status,
                              ingestionMode,
                              notes: source.notes ?? null,
                              ...(source.config ?? {}),
                          },
                      });

                      const existingLeagueId = await findExistingLeagueId(db, platformId, source);
                      for (const resource of source.resources ?? []) {
                          const isConcreteLeagueResource = source.ingestion?.mode === 'league-config'
                              && resource.resourceType === 'league'
                              && resource.externalId === source.ingestion.externalId;
                          await upsertSourceResource(db, {
                              sourceInstanceId: sourceInstance.id,
                              resourceType: resource.resourceType,
                              externalId: resource.externalId,
                              adapterVersion: `${source.adapterKey}-v1`,
                              name: resource.name ?? null,
                              publicUrl: resource.publicUrl ?? source.baseUrl,
                              refreshPolicy: resource.refreshPolicy ?? {},
                              enabled: resource.enabled ?? enabled,
                              leagueId: isConcreteLeagueResource ? existingLeagueId : null,
                          });
                      }

                      sourceCount += 1;
                  }
              }

              options.logger?.info?.(
                  `bootstrapTerritorySourceCatalog: registered ${sourceCount} sources across ${manifests.length} territories`,
              );
              return {
                  sourceCount,
                  enabledLegacyLeagueNames: Array.from(enabledLegacyLeagueNames).sort(),
              };
          }

          /**
           * Links concrete territory league resources to the stable league rows produced by
           * bootstrapLeagueConfigs. This makes provenance navigable without changing league,
           * season, competition, or raw-scrape identities.
           */
          export async function linkTerritoryLeagueResources(db: Kysely<Database>): Promise<number> {
              let linked = 0;
              for (const manifest of readTerritoryManifests()) {
                  for (const source of manifest.sources) {
                      if (source.ingestion?.mode !== 'league-config' || source.enabled !== true) continue;

                      const platform = await db
                          .selectFrom('platforms')
                          .select('id')
                          .where('name', '=', source.platformName)
                          .executeTakeFirst();
                      if (!platform) continue;

                      const league = await db
                          .selectFrom('leagues')
                          .select('id')
                          .where('platform_id', '=', platform.id)
                          .where('external_id', '=', source.ingestion.externalId)
                          .executeTakeFirst();
                      if (!league) continue;

                      const sourceInstance = await db
                          .selectFrom('source_instances')
                          .select('id')
                          .where('platform_id', '=', platform.id)
                          .where('key', '=', source.key)
                          .executeTakeFirst();
                      if (!sourceInstance) continue;

                      await db
                          .updateTable('source_resources')
                          .set({
                              league_id: league.id,
                              updated_at: new Date(),
                          })
                          .where('source_instance_id', '=', sourceInstance.id)
                          .where('resource_type', '=', 'league')
                          .where('external_id', '=', source.ingestion.externalId)
                          .execute();
                      linked += 1;
                  }
              }
              return linked;
          }

          export const __internal = {
              parseTerritoryManifest,
          };
          '''.replace('\n          ', '\n').lstrip())

          all_targets_path = root / 'apps/worker/src/all-scrape-targets.ts'
          all_targets_path.write_text(r'''import type { Kysely } from 'kysely';
          import type { Database } from '@tt-players/db';
          import {
              bootstrapLeagueConfigs,
              readLegacyLeagueConfigs,
              type BootstrapOptions,
              type ScrapeTarget,
          } from './bootstrap.js';
          import { bootstrapNationalTTLeagues } from './national-ttleagues.js';
          import {
              bootstrapTerritorySourceCatalog,
              linkTerritoryLeagueResources,
              readTerritoryLeagueConfigs,
              readTerritoryOwnedLeagueExternalIds,
          } from './territory-source-catalog.js';

          interface TargetLogger {
              info?: (message: string) => void;
              warn?: (message: string) => void;
          }

          export interface ResolveAllScrapeTargetsOptions extends BootstrapOptions {
              logger?: TargetLogger;
          }

          function assertNoDuplicateConfiguredTargets(targets: ScrapeTarget[]): void {
              const seen = new Set<string>();
              for (const target of targets) {
                  const identity = [
                      target.platformType,
                      target.competitionId,
                      target.divisionExtId,
                      target.url,
                      target.isHistorical ? 'historical' : 'current',
                  ].join('|');
                  if (seen.has(identity)) {
                      throw new Error(`Duplicate configured scrape target resolved: ${identity}`);
                  }
                  seen.add(identity);
              }
          }

          export async function resolveAllScrapeTargets(
              db: Kysely<Database>,
              options: ResolveAllScrapeTargetsOptions = {},
          ): Promise<ScrapeTarget[]> {
              const territoryCatalog = await bootstrapTerritorySourceCatalog(db, { logger: options.logger });
              const territoryLeagueConfigs = readTerritoryLeagueConfigs();
              const territoryOwnedExternalIds = new Set(readTerritoryOwnedLeagueExternalIds());
              const legacyLeagueConfigs = readLegacyLeagueConfigs().filter(
                  (league) => !territoryOwnedExternalIds.has(league.externalId),
              );

              const configuredTargets = await bootstrapLeagueConfigs(
                  db,
                  [...legacyLeagueConfigs, ...territoryLeagueConfigs],
                  options,
              );
              assertNoDuplicateConfiguredTargets(configuredTargets);
              await linkTerritoryLeagueResources(db);

              const configuredLeagueNames = new Set(configuredTargets.map((target) => target.leagueName));
              const requestedLeagueNames = options.leagueNames && options.leagueNames.length > 0
                  ? new Set(options.leagueNames)
                  : null;
              const requiredLegacyLeagueNames = territoryCatalog.enabledLegacyLeagueNames.filter(
                  (leagueName) => !requestedLeagueNames || requestedLeagueNames.has(leagueName),
              );
              const missingTerritoryTargets = requiredLegacyLeagueNames.filter(
                  (leagueName) => !configuredLeagueNames.has(leagueName),
              );
              if (missingTerritoryTargets.length > 0) {
                  throw new Error(
                      'Enabled territory legacy-config sources have no operational scrape targets: '
                      + missingTerritoryTargets.join(', '),
                  );
              }

              const nationalTargets = await bootstrapNationalTTLeagues(db, {
                  includeHistory: options.includeHistory,
                  logger: options.logger,
              });
              return [...configuredTargets, ...nationalTargets];
          }
          '''.replace('\n          ', '\n').lstrip())

          test_path = root / 'apps/worker/src/__tests__/territory-source-catalog.test.ts'
          test_path.write_text(r'''import { describe, expect, it } from 'vitest';
          import { parseTerritoryManifest, territoryManifestToLeagueConfigs } from '../territory-source-catalog.js';

          describe('territory source catalog', () => {
              it('parses supported source and resource metadata', () => {
                  const manifest = parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [{
                          key: 'example',
                          name: 'Example source',
                          platformName: 'Example platform',
                          platformBaseUrl: 'https://example.test',
                          baseUrl: 'https://example.test/results',
                          adapterKey: 'example',
                          status: 'discovery',
                          resources: [{
                              resourceType: 'event-results',
                              externalId: 'event-1',
                          }],
                      }],
                  }));

                  expect(manifest.territory).toBe('Scotland');
                  expect(manifest.sources[0]?.resources?.[0]?.resourceType).toBe('event-results');
              });

              it('parses an explicitly schedulable legacy-config source', () => {
                  const manifest = parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [{
                          key: 'example',
                          name: 'Example source',
                          platformName: 'TableTennis365',
                          platformBaseUrl: 'https://www.tabletennis365.com',
                          baseUrl: 'https://www.tabletennis365.com/Example',
                          adapterKey: 'tt365',
                          status: 'active',
                          enabled: true,
                          ingestion: {
                              mode: 'legacy-config',
                              legacyLeagueName: 'Example Table Tennis League',
                          },
                      }],
                  }));

                  expect(manifest.sources[0]?.enabled).toBe(true);
                  expect(manifest.sources[0]?.ingestion?.mode).toBe('legacy-config');
              });

              it('parses concrete league-config sources into the existing bootstrap shape', () => {
                  const manifest = parseTerritoryManifest(JSON.stringify({
                      territory: 'England',
                      sources: [{
                          key: 'example-ttl',
                          name: 'Example Table Tennis League',
                          platformName: 'TT Leagues',
                          platformBaseUrl: 'https://ttleagues-api.azurewebsites.net/api',
                          baseUrl: 'https://example.ttleagues.com',
                          adapterKey: 'ttleagues',
                          status: 'active',
                          enabled: true,
                          ingestion: {
                              mode: 'league-config',
                              externalId: 'example-ttl',
                              seasonName: '2025-26',
                              seasonExtId: '2025-26',
                              history: { enabled: true, maxSeasons: 6, includeCups: false },
                              regions: ['England', 'Example'],
                              divisions: [{ name: 'Division 1', divisionId: 123 }],
                          },
                          resources: [{
                              resourceType: 'league',
                              externalId: 'example-ttl',
                              publicUrl: 'https://example.ttleagues.com',
                          }],
                      }],
                  }));

                  expect(territoryManifestToLeagueConfigs(manifest)).toEqual([{
                      platform: 'ttleagues',
                      leagueName: 'Example Table Tennis League',
                      externalId: 'example-ttl',
                      seasonName: '2025-26',
                      seasonExtId: '2025-26',
                      baseUrl: 'https://example.ttleagues.com',
                      divisions: [{ name: 'Division 1', divisionId: 123 }],
                      history: { enabled: true, maxSeasons: 6, includeCups: false },
                      regions: ['England', 'Example'],
                  }]);
              });

              it('rejects an unknown resource type before it reaches the database', () => {
                  expect(() => parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [{
                          key: 'example',
                          name: 'Example source',
                          platformName: 'Example platform',
                          platformBaseUrl: 'https://example.test',
                          baseUrl: 'https://example.test/results',
                          adapterKey: 'example',
                          status: 'active',
                          resources: [{
                              resourceType: 'made-up-type',
                              externalId: 'event-1',
                          }],
                      }],
                  }))).toThrow(/resourceType is invalid/);
              });

              it('rejects unknown ingestion statuses', () => {
                  expect(() => parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [{
                          key: 'example',
                          name: 'Example source',
                          platformName: 'Example platform',
                          platformBaseUrl: 'https://example.test',
                          baseUrl: 'https://example.test/results',
                          adapterKey: 'example',
                          status: 'maybe',
                      }],
                  }))).toThrow(/status is invalid/);
              });

              it('rejects enabled sources without a schedulable ingestion mode', () => {
                  expect(() => parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [{
                          key: 'example',
                          name: 'Example source',
                          platformName: 'Example platform',
                          platformBaseUrl: 'https://example.test',
                          baseUrl: 'https://example.test/results',
                          adapterKey: 'example',
                          status: 'active',
                          enabled: true,
                      }],
                  }))).toThrow(/cannot be enabled without a schedulable ingestion mode/);
              });

              it('rejects duplicate source, resource, and concrete league identities', () => {
                  expect(() => parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [
                          {
                              key: 'example',
                              name: 'Example source',
                              platformName: 'Example platform',
                              platformBaseUrl: 'https://example.test',
                              baseUrl: 'https://example.test/results',
                              adapterKey: 'example',
                              status: 'discovery',
                          },
                          {
                              key: 'example',
                              name: 'Duplicate source',
                              platformName: 'Example platform',
                              platformBaseUrl: 'https://example.test',
                              baseUrl: 'https://example.test/other',
                              adapterKey: 'example',
                              status: 'discovery',
                          },
                      ],
                  }))).toThrow(/key is duplicated/);

                  expect(() => parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [{
                          key: 'example',
                          name: 'Example source',
                          platformName: 'Example platform',
                          platformBaseUrl: 'https://example.test',
                          baseUrl: 'https://example.test/results',
                          adapterKey: 'example',
                          status: 'discovery',
                          resources: [
                              { resourceType: 'event', externalId: 'event-1' },
                              { resourceType: 'event', externalId: 'event-1' },
                          ],
                      }],
                  }))).toThrow(/duplicates resource/);
              });

              it('rejects invalid concrete league platform and division metadata', () => {
                  expect(() => parseTerritoryManifest(JSON.stringify({
                      territory: 'England',
                      sources: [{
                          key: 'broken',
                          name: 'Broken league',
                          platformName: 'TT Leagues',
                          platformBaseUrl: 'https://ttleagues-api.azurewebsites.net/api',
                          baseUrl: 'https://broken.ttleagues.com',
                          adapterKey: 'ttleagues',
                          status: 'active',
                          enabled: true,
                          ingestion: {
                              mode: 'league-config',
                              externalId: 'broken-ttl',
                              seasonName: '2025-26',
                              seasonExtId: '2025-26',
                              divisions: [{ name: 'Division 1', divisionId: 0 }],
                          },
                          resources: [{ resourceType: 'league', externalId: 'broken-ttl' }],
                      }],
                  }))).toThrow(/divisionId must be a positive integer/);
              });

              it('rejects invalid source URLs', () => {
                  expect(() => parseTerritoryManifest(JSON.stringify({
                      territory: 'Scotland',
                      sources: [{
                          key: 'example',
                          name: 'Example source',
                          platformName: 'Example platform',
                          platformBaseUrl: 'not-a-url',
                          baseUrl: 'https://example.test/results',
                          adapterKey: 'example',
                          status: 'discovery',
                      }],
                  }))).toThrow(/must be an absolute URL/);
              });
          });
          '''.replace('\n          ', '\n').lstrip())

          integration_path = root / 'apps/worker/src/__tests__/territory-source-catalog.integration.test.ts'
          integration = integration_path.read_text()
          old = '''        const enabledSources = await db
              .selectFrom('source_instances')
              .select(['key', 'enabled'])
              .where('enabled', '=', true)
              .orderBy('key', 'asc')
              .execute();
          expect(enabledSources.map((source) => source.key)).toEqual([
              'dumfries-tt365',
              'perth-tt365',
              'west-of-scotland-tt365',
          ]);
          '''
          new = '''        const enabledScotlandSources = await db
              .selectFrom('source_instances')
              .select(['key', 'enabled'])
              .where('enabled', '=', true)
              .where('key', 'in', [
                  'dumfries-tt365',
                  'perth-tt365',
                  'west-of-scotland-tt365',
              ])
              .orderBy('key', 'asc')
              .execute();
          expect(enabledScotlandSources.map((source) => source.key)).toEqual([
              'dumfries-tt365',
              'perth-tt365',
              'west-of-scotland-tt365',
          ]);
          '''
          if old not in integration:
              raise SystemExit('missing territory integration expectation block')
          integration_path.write_text(integration.replace(old, new, 1))

          parity_path = root / 'apps/worker/src/__tests__/england-territory-parity.integration.test.ts'
          parity_path.write_text(r'''import { afterAll, beforeAll, describe, expect, it } from 'vitest';
          import { Kysely, Migrator, PostgresDialect } from 'kysely';
          import type { Migration, MigrationProvider } from 'kysely';
          import pg from 'pg';
          import type { Database } from '@tt-players/db';
          import * as m001 from '@tt-players/db/src/migrations/001_create_enums.js';
          import * as m002 from '@tt-players/db/src/migrations/002_create_core_tables.js';
          import * as m009 from '@tt-players/db/src/migrations/009_create_regions.js';
          import * as m029 from '@tt-players/db/src/migrations/029_create_source_registry.js';
          import { bootstrapLeagueConfigs, readLegacyLeagueConfigs } from '../bootstrap.js';
          import {
              bootstrapTerritorySourceCatalog,
              linkTerritoryLeagueResources,
              readTerritoryLeagueConfigs,
              readTerritoryOwnedLeagueExternalIds,
          } from '../territory-source-catalog.js';

          const { Pool } = pg;
          const TEST_DB_NAME = 'tt_england_territory_parity_test';
          const ADMIN_DATABASE_URL = 'postgres://postgres:postgres@localhost:5432/postgres';
          const TEST_DATABASE_URL = `postgres://postgres:postgres@localhost:5432/${TEST_DB_NAME}`;

          class StaticMigrationProvider implements MigrationProvider {
              async getMigrations(): Promise<Record<string, Migration>> {
                  return {
                      '001_create_enums': m001,
                      '002_create_core_tables': m002,
                      '009_create_regions': m009,
                      '029_create_source_registry': m029,
                  };
              }
          }

          let db: Kysely<Database>;

          async function createTestDatabase(): Promise<void> {
              const adminPool = new Pool({ connectionString: ADMIN_DATABASE_URL });
              await adminPool.query(`DROP DATABASE IF EXISTS ${TEST_DB_NAME}`);
              await adminPool.query(`CREATE DATABASE ${TEST_DB_NAME}`);
              await adminPool.end();
          }

          async function dropTestDatabase(): Promise<void> {
              if (db) await db.destroy();
              const adminPool = new Pool({ connectionString: ADMIN_DATABASE_URL });
              await adminPool.query(`
                  SELECT pg_terminate_backend(pg_stat_activity.pid)
                  FROM pg_stat_activity
                  WHERE pg_stat_activity.datname = '${TEST_DB_NAME}'
                    AND pid <> pg_backend_pid()
              `);
              await adminPool.query(`DROP DATABASE IF EXISTS ${TEST_DB_NAME}`);
              await adminPool.end();
          }

          beforeAll(async () => {
              await createTestDatabase();
              db = new Kysely<Database>({
                  dialect: new PostgresDialect({
                      pool: new Pool({ connectionString: TEST_DATABASE_URL }),
                  }),
              });
              const migrator = new Migrator({ db, provider: new StaticMigrationProvider() });
              const { error } = await migrator.migrateToLatest();
              if (error) throw error;
          });

          afterAll(async () => {
              await dropTestDatabase();
          });

          describe('England territory migration parity', () => {
              it('reproduces every legacy England config and target without changing stable rows', async () => {
                  const legacyEngland = readLegacyLeagueConfigs().filter(
                      (league) => league.regions?.includes('England') === true,
                  );
                  const territoryEngland = readTerritoryLeagueConfigs().filter(
                      (league) => league.regions?.includes('England') === true,
                  );

                  expect(legacyEngland.length).toBeGreaterThan(0);
                  expect(territoryEngland).toEqual(legacyEngland);
                  expect(readTerritoryOwnedLeagueExternalIds()).toEqual(
                      [...legacyEngland.map((league) => league.externalId)].sort(),
                  );

                  await bootstrapTerritorySourceCatalog(db);
                  const territoryKeys = territoryEngland.map((league) => league.externalId);
                  const englandSources = await db
                      .selectFrom('source_instances')
                      .select(['id', 'key', 'enabled'])
                      .where('key', 'in', territoryKeys)
                      .execute();
                  expect(englandSources).toHaveLength(territoryEngland.length);
                  expect(englandSources.every((source) => source.enabled)).toBe(true);

                  const legacyTargets = await bootstrapLeagueConfigs(db, legacyEngland);
                  const leagueIdsBefore = await db
                      .selectFrom('leagues')
                      .select(['external_id', 'id'])
                      .where('external_id', 'in', territoryKeys)
                      .orderBy('external_id', 'asc')
                      .execute();

                  const territoryTargets = await bootstrapLeagueConfigs(db, territoryEngland);
                  expect(territoryTargets).toEqual(legacyTargets);

                  const targetIdentities = territoryTargets.map((target) => [
                      target.platformType,
                      target.competitionId,
                      target.divisionExtId,
                      target.url,
                      target.fixturesUrl ?? '',
                  ].join('|'));
                  expect(new Set(targetIdentities).size).toBe(targetIdentities.length);

                  const leagueIdsAfter = await db
                      .selectFrom('leagues')
                      .select(['external_id', 'id'])
                      .where('external_id', 'in', territoryKeys)
                      .orderBy('external_id', 'asc')
                      .execute();
                  expect(leagueIdsAfter).toEqual(leagueIdsBefore);

                  const linkedCount = await linkTerritoryLeagueResources(db);
                  expect(linkedCount).toBe(territoryEngland.length);
                  const linkedResources = await db
                      .selectFrom('source_resources as sr')
                      .innerJoin('source_instances as si', 'si.id', 'sr.source_instance_id')
                      .select(['si.key', 'sr.external_id', 'sr.league_id'])
                      .where('si.key', 'in', territoryKeys)
                      .where('sr.resource_type', '=', 'league')
                      .execute();
                  expect(linkedResources).toHaveLength(territoryEngland.length);
                  expect(linkedResources.every((resource) => resource.league_id !== null)).toBe(true);

                  const sourceCountBefore = await db
                      .selectFrom('source_instances')
                      .select(({ fn }) => fn.countAll<number>().as('count'))
                      .executeTakeFirstOrThrow();
                  const resourceCountBefore = await db
                      .selectFrom('source_resources')
                      .select(({ fn }) => fn.countAll<number>().as('count'))
                      .executeTakeFirstOrThrow();
                  await bootstrapTerritorySourceCatalog(db);
                  const sourceCountAfter = await db
                      .selectFrom('source_instances')
                      .select(({ fn }) => fn.countAll<number>().as('count'))
                      .executeTakeFirstOrThrow();
                  const resourceCountAfter = await db
                      .selectFrom('source_resources')
                      .select(({ fn }) => fn.countAll<number>().as('count'))
                      .executeTakeFirstOrThrow();
                  expect(Number(sourceCountAfter.count)).toBe(Number(sourceCountBefore.count));
                  expect(Number(resourceCountAfter.count)).toBe(Number(resourceCountBefore.count));
              });
          });
          '''.replace('\n          ', '\n').lstrip())

          config_dir = root / 'apps/worker/config'
          merged = {}
          origins = {}
          for filename in ('leagues.json', 'uk-leagues.generated.json'):
              leagues = json.loads((config_dir / filename).read_text())
              for league in leagues:
                  external_id = league['externalId']
                  if external_id not in merged:
                      merged[external_id] = league
                      origins[external_id] = filename

          england = [
              league for league in merged.values()
              if 'England' in (league.get('regions') or [])
          ]
          if not england:
              raise SystemExit('no England league configs found')

          def source_for(league):
              is_tt365 = league['platform'] == 'tt365'
              ingestion = {
                  'mode': 'league-config',
                  'externalId': league['externalId'],
                  'seasonName': league['seasonName'],
                  'seasonExtId': league['seasonExtId'],
              }
              if 'history' in league:
                  ingestion['history'] = league['history']
              if 'regions' in league:
                  ingestion['regions'] = league['regions']
              ingestion['divisions'] = league['divisions']
              return {
                  'key': league['externalId'],
                  'name': league['leagueName'],
                  'platformName': 'TableTennis365' if is_tt365 else 'TT Leagues',
                  'platformBaseUrl': (
                      'https://www.tabletennis365.com'
                      if is_tt365
                      else 'https://ttleagues-api.azurewebsites.net/api'
                  ),
                  'baseUrl': league['baseUrl'],
                  'adapterKey': league['platform'],
                  'status': 'active',
                  'enabled': True,
                  'ingestion': ingestion,
                  'config': {
                      'role': 'local-league',
                      'migration': 'legacy England configuration migrated mechanically to territory control',
                      'migratedFrom': origins[league['externalId']],
                  },
                  'resources': [{
                      'resourceType': 'league',
                      'externalId': league['externalId'],
                      'name': league['leagueName'],
                      'publicUrl': league['baseUrl'],
                      'enabled': True,
                  }],
              }

          england_manifest = {
              'territory': 'England',
              'sources': [source_for(league) for league in england],
          }
          territory_dir = config_dir / 'territories'
          territory_dir.mkdir(parents=True, exist_ok=True)
          (territory_dir / 'england.json').write_text(
              json.dumps(england_manifest, indent=4, ensure_ascii=False) + '\n'
          )
          print(f'Generated England territory manifest with {len(england)} leagues')
          PY

      - name: Commit generated implementation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add apps/worker
          if git diff --cached --quiet; then
            echo 'No implementation changes produced'
            exit 0
          fi
          git commit -m 'feat: migrate England ingestion to territory config'
          git push origin HEAD:agent/issue-228-england-territory
