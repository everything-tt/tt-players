name: Apply final issue 90 formatting

on:
  push:
    branches:
      - fix/issue-90-player-search-page-first

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply final formatting cleanup
        run: python scripts/issue-90-final-format.py

      - uses: pnpm/action-setup@v4
        with:
          version: 10.24.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type-check API
        run: pnpm --filter @tt-players/api build

      - name: Run page-first query-shape regression test
        run: pnpm --filter @tt-players/api exec vitest run src/__tests__/player-search-query-shape.test.ts

      - name: Commit final formatting cleanup
        run: |
          rm scripts/issue-90-final-format.py
          rm .github/workflows/issue-90-final-format.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/api/src/routes/players.ts \
                  apps/api/src/__tests__/search-pagination.integration.test.ts \
                  scripts/issue-90-final-format.py \
                  .github/workflows/issue-90-final-format.yml
          git commit -m "style(api): finalize player search formatting"
          git push origin HEAD
